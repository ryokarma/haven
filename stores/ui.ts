import { defineStore } from 'pinia';

/**
 * UI Store — Gère les états globaux de l'interface Vue
 */
export const useUIStore = defineStore('ui', () => {
    // Vide suite au retour à l'architecture DOM Event Stopping
    return {};
});
